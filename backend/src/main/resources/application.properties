spring.application.name=backend
# Allow Render to set the port via environment variable
server.port=${PORT:8080}

# MongoDB Configuration
# Use environment variable MONGO_URL if available, or default to the Atlas cluster
spring.data.mongodb.uri=${MONGO_URL:mongodb+srv://vijaynetekal28_db_user:OU3vHnMPrgPMOqyc@cluster0.t1k4iti.mongodb.net/apnastudent_db?appName=Cluster0}

# Logging (Optional, useful for debugging)
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
